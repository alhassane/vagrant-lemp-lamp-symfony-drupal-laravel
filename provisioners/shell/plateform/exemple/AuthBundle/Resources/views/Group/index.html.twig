{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Group list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Created_at</th>
                <th>Updated_at_at</th>
                <th>Archive_at</th>
                <th>Enabled</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('dirisi_admin_group_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.createdat %}{{ entity.createdat|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedatat %}{{ entity.updatedatat|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.archiveat %}{{ entity.archiveat|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.enabled }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('dirisi_admin_group_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('dirisi_admin_group_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('dirisi_admin_group_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

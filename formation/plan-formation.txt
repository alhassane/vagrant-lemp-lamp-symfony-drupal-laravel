jour 1:
_________
- gst installation sous apache de symfony (virtualhost, hosts, permission chown, chmod, setacl, usermod)
- gst installation bdd/tables/assets via commandes
- gst commande app/check
- presentation symfony (archit, arbo, conf, routing)
- creation users via commandes
- presentation firewall/autentification/acl
- creation d'un environnement recette (config_recette, bdd recette, app_recette)


jour 2
___________
- gst commandes app/console
- creation de bundles (TestBundle, ToolsBundle) avec commandes
- creation entity Fiche dans TestBundle avec commandes
- generation CRUD pour la gestion de l'entité Fiche avec commande
- gest doctrine entity / annotation / mapping / requêtes avec ORM
- creation de la commande project:create:user qui génère une liste d'utilisateurs à partir d'un fichier csv contenant 
la liste des usernames/pwd avec gestion des transactions doctrine

jour 3
________
- gst twig
- Création de l'onglet Fiche dans le layout qui affiche la liste des fiches et le lien de création d'une nouvelle fiche si connexion
- creation des tags de translation pour le nom du nouveau menu Fiche
- creation d'un service tagé twig.extension avec création de filtres avec test d'utilisation
- gst du Dependancy Injection design pattern
- création du service project.handler.fiche (HandlerFiche.php) avec la methode processUC($NameValue) qui retourne la liste des fiches dont le nom est égale à la valeur
donnée en paramètre.
- création de l'action listbynameAction dans le controller ProjectTestBundle:Fiche  qui appelle la méthode proccessUC 
du service HandlerFiche et qui formatte le résultat sous forme d'une liste dans le fichier twig listbyname.html.twig
- appel du rendu du controller dans le twig principale du menu Fiche qui se trouve dans les onglets du menu principale
- gst listener kernel.request sur la gestion des langues
- gst listener kernel.exception sur la gestion des exception
- gst des listeners en general avec la visualisation via le debugger
- creation d'un subscriber avec les events prePersist/preUpdate/preRemove/postPersist/postUpdate/postRemove avec test sur l'entité Acteur/Fiche

jour 4
________
- creation d'un listener HandlerLogin sur l'event security.interactive_login. on ajoute dans le traitement un cookie après connexion.
- creation d'un listener HanlderLogout sur l'event security.authentication.failure. on supprime le cookie après déconnexion
- gst form
- gst des formulaires de type service
- intro DDD
- creation formulaire avec factory suivant l'architecture du Domain driven design

jour 5
________
- suite creation formulaire avec factory suivant l'architecture du Domain driven design
- gest phpunit
- gest email
- conclusion